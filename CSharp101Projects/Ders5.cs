using System;

namespace CSharp101Projects
{
    // BİR PROJE ALTINDA TOPLANMASINI İSTEDİM DERS DOSYALARININ, BU YÜZDEN BİR MAİN ÇALIŞACAKSA DİĞERLERİNİ YORUM SATIRINA ALDIM
    // ÇALIŞTIRMAK İÇİN YORUM SATIRINI KALDIRABİLİRSİNİZ.
    public class Ders5
    {
        
        
        //public static void Main(string[] args)
        //{
            /*
            try
            {
                Console.WriteLine("Bir sayı giriniz: ");
                int number = Convert.ToInt32(Console.ReadLine());
                Console.WriteLine("Girmiş olduğunuz sayi : " + number);
            }
            catch (Exception exception)
            {
                Console.WriteLine("ERROR: " + exception.Message.ToString());
            }*/
            /*
            finally
            {
                Console.WriteLine("Parselama, Input alma, Yazdırma; başarıyla tamamlandı.");
            }*/

/*
            try
            {
                // Argument Null Exception
                // int a = int.Parse(null);

                // Format Exception
                // int b = int.Parse("test");

                // Overflow Exception
                int wat = int.Parse("-200000000000000");


            }
            catch (ArgumentNullException argumentNullException)
            {
                Console.WriteLine("There was an ArgumentNullException in the try block.");
                Console.WriteLine("----" + argumentNullException.Message.ToString());
            }
            catch (FormatException formatException)
            {
                Console.WriteLine("There was a FormatException in the try block.");
                Console.WriteLine("----" + formatException.Message.ToString());
            }
            catch (OverflowException overflowException)
            {
                Console.WriteLine("There was a OverflowException in the try block.");
                Console.WriteLine("----" + overflowException.Message.ToString());
            }
            finally
            {
                Console.WriteLine("İşlem tamamlandı.");
            }
        }*/
    }
}